# Problem 1
the manager multiprocessing.Manager() is make a shered variables between the proccess and when we update theme it block the proccess when it count the incomming request so it block the proccess

# Problem 2
the log system is makes an over head because when he log i run the GIL so the reqeust is stop until the log is complete

# Problem 3

